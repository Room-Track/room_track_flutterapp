import 'package:flutter/material.dart';

class VersionSettingsPage extends StatelessWidget {
  const VersionSettingsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Version"),
      ),
      body: Center(
        child: Text("Version Page.."),
      ),
    );
  }
}
